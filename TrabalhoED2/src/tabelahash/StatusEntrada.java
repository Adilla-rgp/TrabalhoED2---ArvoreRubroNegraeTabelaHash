package TrabalhoED2.src.tabelahash;

public enum StatusEntrada {
    VAZIA,  // Posição ainda não está sendo usada
    OCUPADA, // Posição contém um elemento válido
    DELETADA  // Posição tinha elemento que foi removido
}
